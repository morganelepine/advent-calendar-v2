export enum ContentType {
    Quote,
    Idea,
    Anecdote,
    Game,
    Quizz
}

export enum GameType {
    Pendu,
    Jeu,
    QuizzCitation,
    QuizzNoel
}

export enum ScoreType {
    ContentOpening,
    GameCorrectAnswer,
    DayOpening,
    FirstLogin
}